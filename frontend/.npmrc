# pnpm configuration
# Store packages in user home directory to avoid polluting project root
store-dir=~/.local/share/pnpm/store

# Use content-addressable store for better deduplication
node-linker=hoisted
